import { VariantStlye } from "../../hooks/useVariant/useVariant.type"

const GroupStyle: VariantStlye<"container"> = {
  container: {
    flexDirection: "row",
    justifyContent: "space-between",
  },
}

export default GroupStyle
